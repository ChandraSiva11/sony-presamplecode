In programming you often need to know if an expression is True or False.

Hello You can evaluate any expression in Python, and get one of two answers, True or False.

When you compare two values, the expression is evaluated and Python returns the Boolean answer:Hello

The boolean builtins are capitalized: True and False.

The object will always return True, unless:

The object is False, Hello

The object is 0

The object is None